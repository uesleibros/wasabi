# Proxy Support (HTTP CONNECT, SOCKS5, NTLM)

Wasabi can tunnel WebSocket connections through HTTP and SOCKS5 proxies,
with optional Basic and NTLM authentication.

## References

- `DoProxyHTTP()`, `DoProxySOCKS5()`, `GenerateNtlmToken()` in
  `Wasabi.bas`.
