# TCP Tuning

Wasabi applies a set of TCP socket options to reduce latency, detect stale
connections, and increase throughput.

## Reference

- `ApplySocketOptions()` and `ResolveAndConnect()` in `Wasabi.bas`.
